__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
*.db
.env
/data/
build/
# Per-deploy stamps written by install/upgrade.sh. Committing either
# would make every machine claim whichever build a developer stamped
# last, in exactly the number the owner checks after an upgrade.
.build_commit
.build_number
